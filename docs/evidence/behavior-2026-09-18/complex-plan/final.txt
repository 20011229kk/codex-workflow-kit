Implemented the duration parser and CLI, added unittest coverage, and marked all tasks complete in `plan.md` with verification evidence.

`python3 -m unittest discover -v` passed all 6 test methods, covering valid conversions, invalid inputs, and CLI output and exit behavior.

No dependencies installed, services accessed, subagents used, or commits made.