Implemented `total(values)`, including returning zero for an empty list.

Validation: `python3 -B -m unittest discover -v` — all 7 tests passed.

Updated `plan.md` to mark the task complete. The UI-color decision remains pending and unchanged. No commits, installations, service access, or subagents were used.