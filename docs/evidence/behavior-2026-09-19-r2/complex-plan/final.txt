Implemented duration parsing and the CLI, including validation and traceback-free errors.

`python3 -m unittest discover -v` passed all 5 test methods. Updated `plan.md` with completed tasks and verification evidence.

No services, dependency installs, global changes, subagents or commits were used.